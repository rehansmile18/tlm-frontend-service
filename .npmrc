# @base-ui/react declares a peerOptional date-fns@^4 while this app pins ^3.6.0, so a plain
# `npm install <anything>` fails to resolve. The sibling tlm-frontend already carries this file
# for the same reason; this app was simply missing it.
legacy-peer-deps=true
